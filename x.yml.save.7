---
- hosts: all
  become: true  
  tasks:
  - name: execute command 
    command: "{{item}}"
    with_items: 
      apt  update
      apt install python3-pip python3-dev nginxgit clone https://github.com/ani1203/learnDjango.git 
